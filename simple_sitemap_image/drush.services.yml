services:
  simple_sitemap_image.commands:
    class: \Drupal\simple_sitemap_image\Commands\SimpleSitemapImageCommands
    arguments: ['@config.factory', '@entity_type.manager', '@simple_sitemap_image.services']
    tags:
      - { name: drush.command }
